.playwright/
._* 
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Environment variables — never commit real secrets
.env
.env.local
.env.*.local
.env.production
.env.staging

# Capacitor: the native projects (android/, ios/) are INTENTIONALLY committed —
# their generated native config is part of the repo. Only the separate Expo app
# under mobile/ is ignored.
mobile/

# Claude Code worktree data
.claude/

# Impeccable design tooling: the installed skill files + lockfile are not
# committed (like node_modules). The generated design CONTEXT is committed:
# PRODUCT.md, DESIGN.md, .impeccable/design.json.
.agents/
skills-lock.json

