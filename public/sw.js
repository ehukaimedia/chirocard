// Minimal service worker to satisfy PWA requirements
self.addEventListener('fetch', (event) => {
    // This is a placeholder fetch handler to satisfy PWA requirements.
    // In a real offline-first app, we would cache assets here.
    // For now, we just let the network request pass through.
    return;
});
